game: chess
version: V2
file_name: test

mcts:
  num_rollout: 64
  mcts_batch_size: 8
  num_selfplay: 4

training:
  duration: 0.05
  steps_per_iter: 64
  train_batch_size: 16

logging:
  verbose: true
  kaggle: false